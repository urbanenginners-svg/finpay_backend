export enum RoleSlugEnum {
  SUPER_ADMIN = 'super-admin',
  ADMIN = 'admin',
  CUSTOMER = 'customer',
  AGENT = 'agent',
  /** @deprecated Legacy sbzee portal slug — kept for existing OTP flows */
  SOURCING_MANAGER = 'sourcing-manager',
  WAREHOUSE_ASSOCIATE = 'warehouse-associate',
}
